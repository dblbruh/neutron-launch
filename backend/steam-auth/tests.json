{
  "tests": [
    {
      "name": "Generate Steam login URL",
      "method": "GET",
      "expectedStatus": 200,
      "expectedBody": {
        "steamLoginUrl": "string",
        "message": "string"
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "Verify Steam user with invalid action",
      "method": "POST", 
      "body": {
        "action": "invalid_action"
      },
      "expectedStatus": 400,
      "expectedBody": {
        "error": "string"
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "Handle OPTIONS request",
      "method": "OPTIONS",
      "expectedStatus": 200,
      "expectedBody": ""
    }
  ]
}